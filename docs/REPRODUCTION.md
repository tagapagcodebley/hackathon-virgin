# Reproduction Guide

Written for someone starting from a clean environment with none of this set up.

## Requirements

- **OS / runtime:** *(e.g. Python 3.12, Node 20, ...)*
- **Dependencies:** *(package manager + lockfile)*
- **Data:** *(what data is required, where it comes from — public/synthetic only, per rules)*
- **Approx runtime:** *(how long each step below takes)*
- **Approx cost:** *(API/agent costs, if any)*

## 1. Setup

```bash
# clone
git clone <repo-url>
cd micro1-hackathon

# install deps
# (fill in once tooling is chosen)
```

## 2. Run the baseline

```bash
# fill in exact command
```

**Expected output:** *(describe what success looks like)*

## 3. Run the advanced solution

```bash
# fill in exact command
```

**Expected output:** *(describe what success looks like)*

## 4. Run the evaluation

```bash
# fill in exact command
```

**Expected output:** *(scores/metrics comparing baseline vs advanced)*

## Troubleshooting

*(Common issues you hit while building, and the fix — saves judges time.)*
