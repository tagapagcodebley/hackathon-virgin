# Fixtures

Synthetic HTML/text page-state snapshots, one per case in
[`../CASES.md`](../CASES.md), named `NN-slug.html` to match. Fully
synthetic — modeling a fictional tennis-court reservation portal, no real
site's markup or data — per the rulebook's ground rule 07.

TODO — built in Stage 2 (cases needed for baseline scoring) and extended
in Stage 3 (cases needed for advanced-only distinctions, e.g. memory and
near-miss criteria).
